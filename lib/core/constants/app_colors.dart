import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFFFFCD00); // 카카오 옐로우
  static const background = Color(0xFF1A1A2E);
  static const surface = Color(0xFF16213E);
  static const surfaceLight = Color(0xFF0F3460);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B0B0);

  // Decision Types
  static const intuit = Color(0xFFD85A30); // Coral
  static const analyst = Color(0xFF1D9E75); // Teal
  static const vibe = Color(0xFFBA7517); // Amber
  static const zen = Color(0xFF7F77DD); // Purple

  // Emotions
  static const emotionTired = Color(0xFF6B7B8D);
  static const emotionExcited = Color(0xFFFF9F43);
  static const emotionStressed = Color(0xFFEE5A6F);
  static const emotionHesitant = Color(0xFF7C83FD);

  // UI
  static const recordingRed = Color(0xFFFF3B30);
  static const userBubble = Color(0xFF533483);
  static const aiBubble = Color(0xFF1E3A5F);
}
